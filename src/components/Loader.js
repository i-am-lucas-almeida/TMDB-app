import styles from "../styles/components/Loader.module.css";

const Loader = () => {

    return (

        <div className={styles.loader__c}>

            <div className={styles.loader}></div>

        </div>

    );

};

export default Loader;